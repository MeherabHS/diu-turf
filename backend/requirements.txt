# ── Runtime dependencies ──────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv>=1.0.1
pydantic[email]>=2.8.0
pyjwt>=2.10.1
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
httpx>=0.27.0
tzdata>=2024.2

# ── PostgreSQL (production) ──────────────────────────────────────────────────
asyncpg>=0.29.0             # async driver — application runtime
sqlalchemy[asyncio]>=2.0.0  # async engine used only by Alembic
alembic>=1.13.0             # schema migrations

# ── SQLite (local development only) ───────────────────────────────────────────
aiosqlite>=0.20.0           # async SQLite — dev fallback when PostgreSQL unavailable

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0               # also used as AsyncTestClient

# ── Removed ───────────────────────────────────────────────────────────────────
# motor==3.3.1       ← MongoDB async driver — REMOVED (PostgreSQL only)
# pymongo==4.5.0     ← MongoDB sync driver  — REMOVED
# emergentintegrations ← removed Phase 2
# boto3, requests-oauthlib, cryptography, pandas, numpy, jq, typer ← unused
